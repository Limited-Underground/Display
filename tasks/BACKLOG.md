# OpenGauge Engineering Backlog

Statuses: `done` means the documented acceptance criteria are evidenced; `planned` means no implementation claim.

## Foundation

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OG-001 | done | Project bootstrap and repository structure | Self-contained directories, agent guide, README, architecture, status, and backlog exist |
| OG-001A | done | Public host validation | GitHub Actions runs the complete Windows host matrix on `main` pushes and pull requests using least-privilege contents access, bounded concurrency/time, explicit UCRT64 GCC discovery, and commit-pinned actions. The current 47-executable matrix passes locally under the same host script |
| OG-001B | done | Cross-project OpenTrail validation | OpenTrail public run `31570072110` passes its complete 90-executable host matrix at commit `9463975`. OpenGauge links this only as separate OpenTrail platform, transport, routing, GPS, persistence, field, and security-prerequisite evidence. It does not prove OpenGauge CAN, ESP-NOW, display/input, target storage, power, critical delivery, or vehicle behavior. Protected target bindings/storage, authenticated rollback, reset/migration, power-cut/wear, durable outbox, the four-person hardware freeze, and physical evidence remain open |
| OG-002 | done | Initial architecture documentation | Roles, layers, safety/failure boundaries, protocols, and architecture gates documented |
| OG-003 | planned | Hardware abstraction contracts | CAN, clock, ESP-NOW, display/touch, storage, power, and logging contracts reviewed with fakes |
| OG-003A | planned | Hardware/use-case inventory | Candidate acquisition inventory plus arrival procedures now record two Waveshare SKU 31262 non-GPS displays, an Espressif DevKitC bench mule, an ordered Wio Tracker L1 Pro MeshCore/GNSS candidate, and the on-hand Veepeak generic OBD-II adapter with recovery/read-only evidence boundaries. Remains planned until exact received units, vehicle, desired signals, CAN interface, wiring, power, and environment constraints are recorded |
| OG-017 | done | Diagnostics/logging foundation | A pointer-free typed 32-event ring provides five-level filtering, monotonic time, reset-reason capture, oldest-first atomic snapshots, overwrite accounting, and 16 saturating CAN/decoder/cache/wireless/render/update/reset counters. Eight host groups plus 100 repeat runs cover lifecycle, filtering, time regression, wrap/order/sequences, saturation, service-owned counters, canonical records, snapshot/clear, restart, and fixed payload shape. Adapter binding, concurrency/timing, formatting, persistence, and production redaction audit remain |

## CAN/J1939 and telemetry core

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OG-004 | done | CAN driver abstraction | A transmit-incapable passive Classical CAN interface and 16-frame fake expose listen policy, canonical frame/capture data, bus/error/overflow metadata, FIFO/drop-newest behavior, filtering, monotonic time, bus-off/hardware failure, and restart. Eight host groups include EEC1 receive-to-decoder/cache integration. Physical controller/transceiver passivity and ESP-IDF binding remain separate gates |
| OG-005 | done | J1939 identifier parser | Five host-test groups validate priority/DP/PF/PS/SA, PDU1 destination and PGN zeroing, PDU2 group extension, the PF boundary, DP=1, and fail-closed standard/out-of-range/J1939-22 identifiers |
| OG-006 | done | PGN decoder registry | A fixed eight-entry registry rejects invalid/noncanonical/duplicate/full/unknown dispatch, revalidates normalized output, and hosts one narrowly bounded EEC1 engine-speed fixture. Five host-test groups cover valid/highest-valid, reserved/out-of-range, error, unavailable, frame/length/capacity, and bad-decoder paths. Licensed/current J1939 data plus captured traffic remain required before a vehicle-support claim |
| OG-007 | done | Normalized signal model | Six host-test groups validate fixed namespaced IDs, typed integer/Boolean values, canonical units and bounds, explicit no-value quality states, protocol-specific J1939/OBD/GPS/synthetic provenance, sample/receive time, and exact stale/clock boundaries |
| OG-008 | done | Telemetry cache/subscriptions | Six cache host-test groups cover fixed 16-signal capacity, revalidation, duplicate/update/conflict/out-of-order rules, per-signal exact stale boundaries, polling state cursors, insufficient output, clear/restart epochs, and four concurrent writers. A decoder integration group proves valid-to-stale-to-unavailable engine speed without a plausible invalid value. On-device locking/performance and network subscriptions remain later gates |
| OG-014 | done | Alarm framework | A fixed 16-rule normalized-signal engine provides inclusive above/below/outside-range thresholds, exact hysteresis/assert-clear debounce, four severities, nonvalid clear/hold/assert policy, latch/active acknowledgement, atomic bounded event output, and rate-bounded reminders. Ten host groups cover signed boundaries, chatter, stale/unavailable no-value behavior, clock/type/unit rejection, lifecycle, counters, and restart. Cache-task/display/export composition and reviewed vehicle thresholds remain |
| OG-014A | done | Cache-to-alarm composition | A cooperative full-state evaluator scans at most 16 cache snapshots every monotonic poll so unchanged values advance debounce, exact stale boundaries, and reminders. It preflights all snapshot/rule type-unit-quality matches, aggregates at most 16 events, skips unruled state, and resets alarm runtime on cache epoch change without inventing a clear. Seven host groups cover lifecycle/capacity, unchanged state, staleness, reminders, epoch reset/reassert, aggregation, and fail-closed poll errors. Target task/display/export composition remains |

## Wireless gauge network

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OG-009 | done | ESP-NOW transport abstraction | An opaque 250-byte encrypted-unicast contract and deterministic fake provide explicit start/peer/send/receive/completion/status behavior; fixed eight-peer and four-frame fake capacities; channel/encryption agreement; latency/RSSI; missing/offline/mismatch/rejection/loss outcomes; buffer preservation; and completion backpressure across eight host-test groups. Radio completion is not an application ACK. ESP-IDF binding, keys, physical traffic, channel coexistence, and capacity measurements remain later gates |
| OG-010 | done | Gateway-to-gauge packet format | An explicit fixed 96-byte `OGT0` codec carries up to three registered signals with canonical type/unit/quality, gateway/session/sequence identity, source age, zeroed reserved/unused bytes, and CRC-32. Eight host-test groups cover a normative golden vector, cache-to-wire no-value rules, malformed/incompatible/corrupt frames, sequence gap/duplicate/out-of-order/wrap/restart behavior, exact receiver-local staleness, and fake encrypted delivery plus loss. Its 10 Hz/eight-peer 7,680 payload-byte/s estimate excludes radio overhead and is not physical evidence |
| OG-010B | done | Telemetry publication scheduler | A cooperative fixed-capacity scheduler holds 8 peers, 8 subscriptions/peer, and 16 latest signals; prioritizes first/quality, deadband change, then periodic refresh; derives exact stale/no-value state; batches 3; and enforces a hard 50 ms peer packet floor. Two-phase prepare/local-queue commit keeps the same sequence after local rejection and advances after acceptance even when injected radio loss creates a receiver gap. Eight host groups cover lifecycle/capacity, coalescing, initial batching, min/deadband/max timing, stale/recovery/unavailable, token/retry, independent peers, and encoded fake transport loss |
| OG-010C | done | Cache-to-radio gateway publisher | A cooperative composition performs bounded cache full/incremental cursor polls, maps only four registered IDs, counts/skips unknowns, safely resets source/publication baselines on cache epoch change, encodes one 96-byte packet, attempts one nonblocking send, and commits local acceptance/rejection. Seven host groups cover initial/unchanged mapping, clear plus same-value reload, same-sequence local retry, paced 3+1 sync, stale without repoll, accepted injected loss followed by an exact receiver gap, and the actual EEC1 `engine.speed` decoder-to-cache-to-wire path |
| OG-010D | done | Bounded gateway telemetry loop | A single-owner cooperative composition drains at most 1-16 CAN frames, decodes at most eight signals/frame, writes/polls the cache once, attempts one enqueue for each of eight peers, and services transport once. Nine host groups cover rollback/restart, drain fairness, bad/unsupported input, EEC1 end-to-end fake delivery, no-value transitions, stale publication during bus-off, same-sequence local retry, and overflow/status propagation. ESP-IDF task/ISR timing and physical adapters remain |
| OG-010E | done | Gauge telemetry receiver/store | A bounded display-side composition admits only configured peer/encrypted/channel/gateway identity, drains at most four datagrams, validates the codec/stream, tracks duplicate/out-of-order/gap/session state, clears old signals on gateway restart, and stores 16 latest signals with source age plus local elapsed-time staleness. Eight host groups cover lifecycle, encrypted delivery/freshness, trust metadata, malformed input, sequence behavior, session reset, drain fairness, and read failures. ESP-IDF RF/key binding and view model remain |
| OG-011 | planned | Gauge pairing and identity | Threat model, discovery/approval, key storage, replacement/reset/revoke/recovery, and peer-limit tests |
| OG-011A | done | Logical peer approval and authorization registry | A fixed eight-peer registry permits one exact local approval window, validates gateway/gauge/GPS/trail-bridge role permissions and channel, stores only opaque secure-key handles/epochs, authorizes peer+handle+channel+permission, and supports revoke/forget/replacement/rotation plus atomic snapshots. Eight host groups plus 100 repeats cover lifecycle, validation, exact timeout, admission/denial, capacity/duplicates, revoke/replace, epoch rotation, and snapshot shape. Discovery/UI/local-presence proof, raw-key storage/crypto, persistent recovery/reset, rate limits, ESP-NOW binding, and physical attack tests remain OG-011 |
| OG-011B | done | Canonical peer-authorization restart checkpoint | A fixed 256-byte `OPA0` record preserves up to eight active/revoked logical peers, role permissions, channel, opaque key handles, and nonzero authorization epochs with exact policy compatibility, canonical padding, and CRC. Export refuses pending approvals; boot-only import validates on private state and commits atomically. Eight groups, the full 33-executable matrix, and 100 repeats pass. Coordinated `OPA0`/`OCR0` recovery, protected target storage, authenticated integrity, rollback protection, and physical power-cut evidence remain |
| OG-011C | done | Recoverable peer-authorization checkpoint store | A fixed 288-byte `OPS0` envelope and two-slot host store wrap exact `OPA0`, allocate monotonically increasing generations, rotate away from the newest good slot, require byte/decode readback, recover the prior generation across ten interrupted-write boundaries, reconcile full-write I/O errors at boot, expose degraded reads, and fail closed on conflict/exhaustion. Ten groups, the full 34-executable matrix, and 100 repeats pass. Coordinated authorization/replay/outbox recovery, protected target storage, authenticated integrity, rollback, and physical power-cut/wear evidence remain |
| OG-010A | planned | Physical ESP-NOW characterization | Packet loss, latency, peers, interference/coexistence, reboot recovery, and update-rate evidence on selected boards |

## Display and persistence

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OG-012 | planned | Gauge rendering framework | View-model boundary and representative numeric/needle/bar/warning/stale screens benchmarked on candidate hardware |
| OG-012B | done | Display-neutral gauge view model | A fixed eight-widget projection validates registered signal/kind/label/stale/range configuration and atomically emits expected type/unit, session/sequence/age, distinct valid/suspect/missing/stale/unavailable/error/out-of-range/unknown state, values only when permitted, and attention flags. Seven host groups cover configuration/lifecycle, missing, valid/suspect, exact stale, unavailable/error metadata, shared signals, and atomic capacity/receiver failure. Rendering/touch/persistence/hardware benchmarks remain OG-012/012A |
| OG-012C | done | Display-neutral gauge trend buffer | Four fixed series retain 2-120 oldest-first points at independent exact minimum intervals, coalesce early samples, bound overwrite, permit shared signals, and encode every nonvalid display state as a gap with no number. Eight host groups plus 100 repeats cover lifecycle/configuration, valid/suspect values, stale/error gaps, interval boundary, ring order, shared-signal independence, malformed/clock atomicity, read capacity, and clear. Renderer/axes/decimation, RAM/timing, task locking, persistence/privacy, and physical display acceptance remain OG-012/012A |
| OG-013 | planned | Persistent gauge configuration | Versioning, validation, migration, safe defaults, write wear, import/reset/recovery tested |
| OG-013A | done | Versioned recoverable gauge layout record | An explicit 576-byte `OGL0` schema-v1 record and two-slot write/readback/byte-compare/decode store validate up to eight runtime widgets, reject malformed/noncanonical/corrupt/stale/conflicting records, rotate slots, preserve one last-known-good layout across partial/corrupt writes, and expose safe-default/I/O recovery. Twelve host groups plus 100 repeats now cover codec, strict and automatic generations, unchanged suppression, selection, wear counts, interruption, verification, reset, conflict, exhaustion, and failure paths. Schema migration, import UX, ESP-IDF binding, physical power-cut/wear evidence, and security remain OG-013 |
| OG-013B | done | `OGL0` key/value target adapter | A shared backend-neutral blob contract now supports both configuration and recovery storage. The layout adapter binds exact 576-byte slots to `og_config` / `gauge_layout` / `ogl0_a|b`, validates arguments and sizes, requires commit after write/present-key erase, preserves idempotent missing-key erase, and lets the real store select state after applied/unapplied failed commits. Ten groups, 100/100 focused repeats, and the complete 43-executable matrix pass. ESP-IDF binding, physical interruption/wear, migration, and configuration authenticity remain |
| OG-013C | done | Store-owned layout generation and write suppression | `save_next_if_changed` treats caller generation as non-authoritative, validates canonical layout content, returns the active generation without writing when content is unchanged, and otherwise allocates highest-valid plus one. Invalid/conflicted/unreadable/exhausted state fails before write. Fake-slot and real key/value adapter tests prove generation 1/2, zero unchanged backend writes/commits, and restart selection. Twelve core and ten adapter groups pass 100/100 focused repeats plus the complete 43-executable matrix. Physical wear savings remain unmeasured |
| OG-013D | done | Typed layout commit uncertainty | Key/value commit I/O failure now maps to distinct storage/store uncertainty rather than ordinary failure. Applied uncertainty is discovered after restart as the new generation and an identical update performs no rewrite; unapplied uncertainty retains the prior generation and the same update writes the next generation once. Ten adapter groups pass 100/100 repeats plus the complete 43-executable matrix. Physical interruption and backend-specific atomicity remain unproved |
| OG-013E | done | Single-use local layout confirmation | One validated layout request may be staged under an exact nonzero ID and monotonic deadline. Exact confirmation consumes it before persistence; mismatch, cancellation, expiry, and clock rollback cannot write. Ordinary and uncertain failures cannot replay approval, and restart inspection suppresses a rewrite after an applied uncertain commit. Nine groups pass 100/100 repeats plus the complete 44-executable matrix. Physical-presence input, rendering, target serialization, authenticated configuration, and hardware evidence remain |
| OG-013F | done | Same-boot layout request replay rejection | Successfully staged request IDs must increase strictly during each coordinator start cycle. Cancelled, failed, uncertain, unchanged, and applied requests consume their ID; invalid and blocked proposals do not. Reuse, rollback, and 32-bit exhaustion fail before staging or writing, while explicit stop/start establishes a new boot-local sequence. Ten cumulative coordinator groups pass 100/100 repeats. Cross-boot input flushing, persistent session identity, and physical target evidence remain |
| OG-013G | done | Display-neutral layout-change operator projection | Coherent coordinator state, one observed operation result, and local time map to a fixed semantic state/action record. A live prompt alone carries the opaque request token and exact remaining time; a rejected stale action is flagged without hiding that valid prompt. Applied/unchanged, retry/reconcile, expiry/clock fault, cancellation, terminal rejection, and unavailable state stay distinct; impossible combinations fail closed. Eight groups pass 100/100 repeats plus the complete 45-executable matrix. Rendering, localization, diagnostic retention, target concurrency, and physical input remain |
| OG-013H | done | Redacted layout-change diagnostic event | A separate magic/versioned 32-bit word records only coarse operator state/action/attention/confirmation/rejected-action plus mandatory redaction. Request ID, countdown, generation, layout content, labels, and counters are structurally absent. Reserved bits, unknown enums, incoherent flags, and malformed live details fail before the fixed diagnostic ring. Eight groups pass 100/100 repeats plus the complete 46-executable matrix. Target logger binding, persistent retention/export, and physical failure capture remain |
| OG-013I | done | Atomic layout-change workflow facade | One facade owns the coordinator and returns operation error, persistence evidence, projection error, and immediate post-operation operator status together. Callers cannot later re-pair mutable status with stale observations. Eight groups pass 100/100 repeats plus the complete 47-executable matrix. A serialized target task/lock, renderer/input, source authorization, diagnostics policy, and hardware binding remain |
| OG-013J | done | Typed layout reset uncertainty | Two-slot reset still attempts both erases but returns `commit_uncertain` if either backend commit is uncertain. Four real key/value restart cases cover first/second erase commit, applied/unapplied: only actually empty storage selects safe default, while an unapplied erase preserves the surviving prior slot. Eleven adapter and twelve core groups pass 100/100 repeats plus the complete 47-executable matrix. Reset confirmation, target atomicity, physical interruption, and endurance remain |
| OG-013K | done | Confirmed restore-default without erase | `stage_restore_default` shares the existing one-pending confirmation/replay/workflow path, validates the compiled default, stores it as the normal next generation, restores it after restart, suppresses unchanged repeat writes, and rejects invalid content. Nine cumulative workflow groups pass 100/100 repeats plus the complete 47-executable matrix, explicitly with zero slot erases. Renderer/input binding, source authorization, target serialization, and physical evidence remain |
| OG-013L | done | Strict confirmed local layout import | `stage_import_record` accepts only one exact canonical 576-byte `OGL0` record, preserves typed codec errors, returns a bounded structural summary, and stages valid content through the existing confirmation path. Invalid input does not consume an ID or disturb a prompt; source generation is non-authoritative; stage performs no write. Ten cumulative workflow groups pass 100/100 focused repeats plus the complete 47-executable matrix. File/UI binding, source authorization/authenticity, and physical evidence remain |
| OG-013M | done | Fail-closed canonical layout export | `export_current` reuses normal load selection and emits one exact canonical `OGL0` record with source/slot/recovery evidence. It may export a validated safe fallback or known surviving slot, but invalid arguments/default, generation conflict, or any unreadable-slot uncertainty leaves output unchanged. Thirteen cumulative layout groups pass 100/100 focused repeats plus the complete 47-executable matrix. File/download binding, access/confidentiality policy, and physical evidence remain |
| OG-012A | planned | Display hardware feasibility | The SKU 31262 two-unit arrival plan defines shipping-demo preservation, exact revision, silicon/flash/security, BOOT/RESET recovery, vendor display/touch/power/IMU baseline, representative valid/warning/unavailable/error/stale rendering, resource/timing/power/heat, independent failure, and restore evidence. No unit has arrived or been tested |

## Modules and lifecycle

| ID | Status | Task | Acceptance evidence |
| --- | --- | --- | --- |
| OG-015 | done | GPS node architecture | A transport-neutral latest-fix tracker validates explicit no-fix/2D/3D/differential/RTK/estimated quality, partial-field presence, integer position/altitude/speed/heading/accuracy/UTC bounds, source age, session/sequence loss and wrap, receiver-local exact staleness, restart, and fail-visible value stripping. Eight host groups plus 100 repeats cover malformed/boundary/lifecycle/loss/time/overflow behavior; topology/update-rate/privacy and the ordered Wio Tracker L1 Pro recovery-first arrival plan are documented. Parser/transport/authentication, physical GNSS accuracy/rate/power/antenna, and integration remain separate gates |
| OG-016 | planned | OTA/recovery architecture | Authenticity, compatibility, partitions, interruption, health confirmation, rollback, fleet sequence, and USB recovery validated |
| OG-016A | done | OTA trial confirmation and rollback guard | A pure bounded lifecycle admits only verified newer exact-hardware inactive-slot candidates, requires full readback plus persisted boot selection, accumulates role-specific health for a minimum stable interval strictly before an exact deadline, bounds trial boots, and requires observed original version/slot to finish rollback. Eight host groups plus 100 repeats cover policy/candidate/write evidence, health/time, timeout, reboot limit, mismatch, explicit rollback, clocks, and lifecycle. Signature/download/write/boot adapters, persistent lifecycle, physical power interruption/rollback/USB recovery, and fleet acceptance remain OG-016 |
| OG-018 | done | OpenTrail alert interface | Mirrored specifications and three normative fixtures define an explicit 64-byte frame for seven alert types, canonical milli-units, assert/clear lifecycle IDs, optional UTC/value fields, and CRC-32. Four OpenGauge exporter scenario groups and nine independent OpenTrail ingress groups round-trip identical bytes and enforce invalid semantics, authenticated/authorized producer context, stale/future time, duplicate/conflict, fixed-rate/emergency-reserve, monotonic-time, and fail-closed producer-capacity behavior. Physical transport, key lifecycle, replay protection, and field delivery remain later gates |
| OG-018A | done | Alarm-to-critical-alert composition | An allowlisted 16-rule exporter maps only final local assert/clear transitions to the existing 64-byte codec, preserves condition IDs, allocates unique event IDs, derives bounded monotonic age, converts six canonical units, and rejects nonvalid assertions, local reminder/ack/latch events, lifecycle conflicts, unsupported/range values, and codec failures without ID/state commit. Seven host groups cover configuration/capacity, round-trip, local-only events, quality/lifecycle, units, codec rollback, freshness/restart/exhaustion. Physical authenticated transport and rule mappings remain |
| OG-018B | done | Critical-alert application-delivery outbox | An eight-entry emergency-reserved outbox validates `OGA0` frames/event uniqueness, prepares oldest emergency then oldest ready critical frame, separates local rejection/acceptance from exact event+condition+lifecycle application ACK, and bounds abandoned tokens, backoff, attempts, lifetime, late ACK, and terminal evidence. Eight host groups plus 100 refined repeats cover malformed/capacity/priority/order, two-phase sends, exact timers, retries/failures, late/mismatched ACK, and clocks. Authenticated ACK codec/transport, persistence, physical Heltec/repeater delivery, and end-to-end OpenTrail acceptance remain |
| OG-018C | done | Mirrored critical-alert ACK codec | Independent OpenGauge and OpenTrail codecs encode/decode an explicit 64-byte `OGK0` accepted/rejected ACK containing consumer/producer/event/condition/lifecycle, boot session, sequence, observed age, canonical reserved bytes, and CRC. Three mirrored normative vectors and four scenario groups per repo pass full host matrices plus 100 repeats. Transport authentication/authorization, replay window/persistence, outbox composition, and physical end-to-end ACK delivery remain |
| OG-018D | done | Critical-alert ACK ingress and correlation | A fixed eight-consumer ingress requires adapter-authenticated metadata, exact peer/key-handle/channel/permission authorization, explicit consumer/boot-session binding, codec/producer/age checks, a 32-sequence replay bitmap, and exact retained event/condition/lifecycle correlation before an accepted ACK removes an outbox entry. Rejected ACKs remain explicit non-success and feed OG-018F. Eight host groups plus 100 repeats cover capacity/lifecycle, accepted completion, transport denial, malformed/identity/session/age, bounded reordering/duplicate/old/ambiguous replay, explicit rebind, negative ACK, mismatch, and clocks. Physical authenticated transport, persistent replay/authorization, and end-to-end hardware evidence remain |
| OG-018E | done | ACK binding/replay checkpoint | A fixed 280-byte `OAI0` record serializes eight consumer/session bindings and 32-sequence replay bitmaps with exact producer identity, authorization epochs, canonical inactive/reserved bytes, and CRC. Export/import revalidate live peer permission+epoch; rotation/revoke invalidates stale state; import is boot-only and atomic. Eight groups plus 100 repeats cover canonical empty/bound state, replay restore, malformed/version/producer/CRC rejection, output/state preservation, rotation/revoke/rebind, import timing, duplicate entries, and replay invariants. OG-018G provides a host storage composition; coordinated authorization persistence, secure rollback protection, and physical power-loss evidence remain |
| OG-018F | done | Negative ACK retry/terminal policy | Exact correlated `OGK0` reasons have one bounded action: rate-limited/internal-error releases the outbox entry after its normal backoff; unauthorized, stale, duplicate, conflict, malformed, and unsupported rejection terminate immediately; a retryable response at the attempt limit also terminates. Typed terminal evidence retains event, condition, canonical remote reason, and attempts. Eight host groups cover all reasons, exact backoff, attempt escalation, evidence, late ACK versus prepared state, atomic invalid calls, lifetime, and replay consumption; policy, ingress, and outbox suites each repeat 100 times. Operator presentation, persistence, and physical authenticated delivery remain |
| OG-018G | done | Recoverable ACK checkpoint store | A fixed 320-byte `OAS0` envelope carries caller-owned generation plus the exact 280-byte `OAI0` payload with canonical padding and CRC. A two-slot host store exports only after generation preflight, targets invalid/empty or older state, requires full readback/byte/decode verification, restores the newest unique record through boot-only ingress import, preserves one good slot after partial/corrupt writes, reports I/O degradation, rejects stale/equal-conflict generations, old authorization epochs, and nested checkpoint tamper, and resets both slots. Ten host groups plus 100 repeats cover these semantics; affected checkpoint and ingress suites also repeat 100 times. ESP-IDF/NVS binding, generation allocation, coordinated authorization/outbox commit, anti-rollback hardware, physical brownout/wear, and secure replacement remain |
| OG-018H | done | External host-mediated alert/ACK wire proof | OpenTrail OT-017D loaded OpenGauge's public normative `OGA0` fixture and completed two role-reversed Heltec/SenseCAP cycles: 2/2 exact alerts and 2/2 correlated `OGK0` ACKs, every field valid, zero loss/duplicates/new errors, 1009.6-1014.0 ms round trips, exact aggregate +4 repeater flood RX/TX, repeat preserved, and 4/4 endpoint cleanup. The host supplied trust and did not run OpenGauge exporter/outbox/ACK-ingress/policy on-device; authenticated target transport, full OpenGauge composition, failures/restarts/revoke, and field evidence remain |
| OG-018I | done | Physical ACK completion through OpenGauge host composition | Two later role-reversed cycles passed the returned physical `OGK0` into real OpenGauge peer authorization, explicit session binding, replay/correlation ingress, and an exact reconstructed in-flight outbox. Both reported processed/accepted, one acknowledgement, completed entry, and zero queued/in-flight state; radio loss/duplicates/errors were zero, round trips were 1005.3-1014.2 ms, SenseCAP aggregate was exact +4 flood RX/TX, and 4/4 cleanup passed. Persistent live on-device state, real protected keys, target transport/storage, negative/failure/restart/revoke cases, and field evidence remain |
| OG-018J | done | Physical terminal stale-rejection policy | Two role-reversed cycles passed exact returned rejected/stale `OGK0` frames through real authorization/session/replay/correlation and terminal rejection policy. Both reported processed but not completed, zero acknowledgements, no retry release, explicit terminal failure, and zero queued/in-flight state; radio loss/duplicates/errors were zero, round trips were 1013.1-1014.7 ms, SenseCAP aggregate was exact +4 flood RX/TX, and cleanup passed 4/4. Retryable rejection and persistent failure/restart/revoke/field cases remain |
| OG-018K | done | Physical retryable rate-limit rejection | Two role-reversed cycles passed exact returned rejected/rate-limited `OGK0` frames through real authorization/session/replay/correlation and retry policy. Both reported processed but not completed, zero acknowledgements, one queued retry, zero in flight, retry release, and no terminal failure; radio loss/duplicates/errors were zero, round trips were 1000.8-1008.3 ms, SenseCAP aggregate was exact +4 flood RX/TX, and cleanup passed 4/4. Persistent retry-to-accept and failure/restart/revoke/field cases remain |
| OG-018L | done | Physical retry-to-accepted completion | Two role-reversed four-leg sequences processed rate-limit rejection, enforced not-ready at 25 ms and byte-identical retry readiness at 26 ms, retransmitted, then completed only after a next-sequence accepted ACK. Both finished with one acknowledgement, one remote retry, zero queued/in-flight/terminal failures, zero application duplicates/new radio errors, exact aggregate +8 SenseCAP flood RX/TX, and 4/4 cleanup. State was reconstructed after both responses; durable live/restart/field cases remain |
| OG-018M | done | Live host state across physical retry lifecycle | One OpenGauge process started with a real in-flight entry before each first radio send and retained authorization/replay/outbox state across rate-limit rejection, exact backoff, same-frame retry, and final accepted ACK in both roles. Both finished with one acknowledgement/retry, zero queued/in-flight/terminal failures, zero application duplicates/new radio errors, exact aggregate +8 SenseCAP flood RX/TX, and 4/4 cleanup. Coordinated durable restart and on-device/field cases remain |
| OG-018N | done | Canonical outbox checkpoint codec | A fixed 640-byte `OOC0` record carries eight queued/in-flight exact frames, attempts, and remaining lifetime/action time with a nonzero configuration fingerprint, canonical zero padding, CRC, duplicate-event rejection, strict state/timing/frame checks, and atomic decode. Seven host groups, the full 29-executable matrix, and 100 focused repeats pass. Outbox export/import, coordinated ACK/authorization generation, two-slot target storage, power-loss, authenticated integrity, and rollback resistance remain |
| OG-018O | done | Live outbox restart reconstruction | Boot-only atomic `OOC0` import/export now reconstructs exact queued retry readiness, in-flight ACK timeout, maximum lifetime, frames, state, and attempts against a new monotonic origin. Prepared sends, corrupt/incompatible records, nonempty runtime, and v0-unrepresentable timers fail closed without partial output/state mutation. Five integration groups join the seven codec groups; the full 29-executable matrix and 100 focused repeats pass. Coordinated ACK/authorization generation, durable two-slot target storage, power-loss, authenticated integrity, and rollback resistance remain |
| OG-018P | done | Canonical outbox policy fingerprint | The nonzero versioned `OCF0` compatibility fingerprint is now derived internally from local-commit timeout, ACK timeout, retry backoff, maximum lifetime, maximum attempts, and emergency reserve. Callers can no longer label different policies as compatible. A thirteenth checkpoint group proves determinism and sensitivity to all six fields; the full 29-executable matrix and 100 focused repeats pass. Coordinated ACK/authorization generation and durable storage remain |
| OG-018Q | done | Coordinated critical-alert recovery envelope | A fixed 960-byte `OCR0` record binds one exact 280-byte `OAI0` ACK replay/authorization checkpoint and one exact 640-byte `OOC0` outbox checkpoint to a single nonzero generation. Canonical header/padding, declared sizes, both nested identities/integrity, outer CRC, and atomic decode pass four groups, the full 30-executable matrix, and 100 repeats. Serialized live dual export, preflighted atomic dual import, recoverable two-slot storage, power-loss, authenticated integrity, and rollback resistance remain |
| OG-018R | done | Preflighted live coordinated recovery | Exclusive-owner live export now emits one `OCR0` only after both component exports succeed. Import decodes once and executes both exact boot imports on private copies before committing the same outbox/replay generation. Five groups, the full 31-executable matrix, and 100 repeats cover exact retry/replay restore plus invalid generation, stopped ingress, prepared outbox, policy mismatch, authorization rotation, and corruption without partial live mutation. Two-slot storage, target/power-loss evidence, authenticated integrity, and rollback resistance remain |
| OG-018S | done | Recoverable coordinated recovery store | A two-slot host store writes exact 960-byte `OCR0` records, requires increasing generations and full readback/byte/decode verification, restores the newest unique valid generation through preflighted dual import, preserves the prior good slot after partial/corrupt writes, reports degraded I/O, and fails closed on equal-generation conflict. Eight groups, the full 32-executable matrix, and 100 repeats pass. ESP-IDF/NVS binding, generation allocation, physical power-cut/wear, authenticated integrity, protected authorization persistence, and rollback resistance remain |
| OG-018T | done | Store-owned recovery generation allocation | Normal save now derives generation as highest-valid plus one under exclusive store ownership, starts at 1 on empty media, rotates monotonically across slots, refuses unreadable/conflicted baselines, and reports 64-bit exhaustion before export or write. Ten store groups, the full 32-executable matrix, and 100 repeats pass. Factory-reset/replacement authority, target NVS, physical power-cut/wear, authenticated integrity, protected authorization persistence, and rollback resistance remain |
| OG-018U | done | Uncertain commit and interruption reconciliation | Every storage write error now reports conservative `commit_uncertain` evidence with intended slot/generation. Sixteen interrupted overwrite lengths across `OCR0` layout boundaries preserve the prior good generation, while a complete write followed by I/O failure is selected as committed during boot reconciliation. Twelve store groups, the full 32-executable matrix, and 100 repeats pass. Target-backend atomicity, physical power cuts/wear, authenticated integrity, and rollback resistance remain |
| OG-018V | done | Atomic authorization plus alert-system recovery | A fixed 1280-byte `ORS0` record binds exact `OPA0` peer authorization and exact `OCR0` ACK/outbox state to one generation. Import restores authorization/outbox only in private candidates and constructs a temporary ACK ingress against those candidates, so epoch, policy, pointer, replay, and retry dependencies preflight before any of three live owners changes. Six groups, the full 35-executable matrix, and 100 repeats pass. Recoverable `ORS0` storage, target binding, authenticated integrity, rollback authority, physical power cuts, and on-device boot composition remain |
| OG-018W | done | Recoverable alert-system recovery store | A two-slot host store writes exact 1280-byte `ORS0` generations, owns normal monotonic allocation, preserves the newest good slot across eleven interrupted-write boundaries, requires exact readback/decode, reconciles a full write followed by I/O error at boot, exposes degraded reads, and fails closed on conflict/exhaustion. Eight groups, the full 36-executable matrix, and 100 repeats pass. ESP-IDF protected storage, authenticated integrity, rollback/reset authority, physical power cuts/wear, and on-device boot composition remain |
| OG-018X | done | Trusted generation-floor contract | `restore_at_or_above` refuses an old-but-valid newest `ORS0` below a caller-supplied trusted minimum before any live owner import; `save_next_after` allocates beyond both the caller's last-trusted generation and every valid local slot, with pre-write exhaustion. Ten focused store groups, the unchanged 36-executable matrix, and 100 repeats pass. Selection/persistence of a hardware-backed trusted value, authenticated integrity, reset/replacement authority, and physical evidence remain |
| OG-018Y | partial | Target system-recovery adapter | The target plan defines exact two-slot `ORS0` semantics, protected opaque-handle resolution, a separate trusted-generation source, boot/save ordering, coordinated reset/replacement, and power-cut/endurance evidence. OG-018AG supplies the backend-neutral key/value byte adapter and OG-018AH composes real boot/save policy through it across restart uncertainty. No exact ESP-IDF target/backend, protected key store, monotonic trust source, physical interruption, or on-device boot result exists yet |
| OG-018Z | done | Protected key-handle recovery preflight | Direct and two-slot `ORS0` restore accept an injected validator after private authorization import but before outbox/ACK preflight or live mutation. Only active logical peers and opaque handles are checked; revoked entries are skipped. Unavailable, purpose-mismatch, and backend failures retain typed peer-specific evidence and leave all three live owners unchanged. Eight system groups, eleven store groups, the unchanged 36-executable matrix, and 100 repeats each pass. Concrete protected storage and on-device boot evidence remain |
| OG-018AA | done | Typed system-recovery boot coordinator | A host coordinator combines provisioning state, trusted-generation reads, non-mutating two-slot inspection, protected-key `ORS0` restore, and exact trusted-floor catch-up. Clean first boot requires uninitialized trust, explicitly unprovisioned state, and two empty slots. Restored/degraded/safe-mode/service outcomes stay distinct, and transport remains disabled until recovery and trust reconciliation succeed. Nine groups, the complete 37-executable matrix, and 100 repeats pass. Target task composition, real protected storage/trust, physical interruption, and operator recovery remain |
| OG-018AB | done | Verified system-recovery save ordering | A host coordinator requires exact local/trusted generation agreement before normal save, verifies the next `ORS0`, advances trust only after commit, and requires exact trust readback. Missing local state, rollback, conflict, storage/export failure, uncertain commit, local-ahead reconciliation, and trust-update failure remain typed and never report committed. Eight groups, the complete 38-executable matrix, and 100 repeats pass. Target storage/trust, physical interruption, endurance, provisioning/reset, and operator recovery remain |
| OG-018AC | done | Unreadable-slot boot fail-close | A surviving valid `ORS0` beside known empty/invalid media may restore operationally degraded, but an unreadable peer slot may conceal a newer committed generation. The boot coordinator now retains any visible restore only under exclusive ownership, returns service-required, does not advance trust, and keeps transport disabled. Ten boot groups, the complete 38-executable matrix, and 100 repeats pass. Physical backend diagnosis and operator recovery remain |
| OG-018AD | done | Known-degraded recovery repair | A repair coordinator accepts only current operational `restored_degraded` evidence with matching active/trusted generation, one valid slot, and one known empty/invalid peer. It commits the next `ORS0`, advances/readbacks trust, and verifies both slots valid. Healthy, unreadable, service, stale, and uncertain cases cannot report repaired; uncertainty routes to reboot reconciliation. Five groups, the complete 39-executable matrix, and 100 repeats pass. Physical durability, scheduling, audit, and unreadable-media service remain |
| OG-018AE | done | Redacted recovery operator status | Boot, save, and repair outcomes map into one trivially copyable record bounded to 40 bytes. Coarse state/reason/action, slot health, generations, key-failure category, and transport/repair flags remain visible; peer IDs, key handles, addresses, credentials, and raw checkpoint data are structurally absent. Unknown or inconsistent input fails closed. Seven groups, the complete 40-executable matrix, and 100 repeats pass locally. Target logging/rendering, audit retention, and physical service UX remain |
| OG-018AF | done | Versioned recovery diagnostic event | One atomic 32-bit word records the redacted recovery operation, state/reason/action, slot health, key-failure class, and transport/attention/repair/redaction flags through the existing fixed ring. Magic/version and coherence checks reject malformed words; generations and identity-bearing fields are omitted. Eight groups, the complete 43-executable matrix, and 100 repeats pass locally. Target logger binding, persistent retention/export, and physical service capture remain |
| OG-018AG | done | `ORS0` key/value target adapter | A backend-neutral adapter binds the two exact 1280-byte recovery slots to `og_state` / `og_recovery` / `ors0_a|b`, rejects invalid arguments and wrong-sized values, requires explicit backend commit after write/present-key erase, treats missing-key erase as idempotent, and leaves failed commits for restart inspection. Real store save/rotation/reset and applied/unapplied failed-commit restart discovery pass. Nine groups, 100/100 focused repeats, and the complete 43-executable host matrix pass. ESP-IDF binding, protected keys, authenticated integrity, trusted generation, target locking, physical interruption/endurance, authorized replacement, and on-device evidence remain |
| OG-018AH | done | `ORS0` adapter boot/save composition | Real boot and verified-save coordinators now run through the key/value adapter and restarted store instances. Normal one-slot and two-slot restarts restore exact state; an applied uncertain save advances separate trust only after restart inspection, while an unapplied uncertain save restores the prior trusted generation. Thirteen adapter groups, 100/100 focused repeats, and the complete 43-executable host matrix pass. Trust remains a separate injected fake: no protected monotonic backend, ESP-IDF target, physical power cut, or on-device result is claimed |
| OG-019 | planned | APU/auxiliary boundary | Telemetry-only scope first; any control has separate auth, replay, interlock, fail-safe, and hazard review |

## Recommended sequence

Complete the missing vehicle/power/CAN portions of OG-003A while binding the
completed OG-010D loop to selected ESP-IDF CAN and radio adapters without confusing a
host-tested interface with on-device or RF acceptance.
Incoming display work may begin with OG-012A vendor-example and recovery
evidence, but does not bypass the normalized-data path.
